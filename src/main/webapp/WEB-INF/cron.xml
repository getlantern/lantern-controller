<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
  <cron>
    <url>/persist</url>
    <description>Persist memcache entries to datastore</description>
    <!-- this must match the value in ShardedCounterManager.PERSIST_TIMEOUT -->
    <schedule>every 1 minutes</schedule>
  </cron>
  <cron>
    <url>/check_sqs</url>
    <description>Check the incoming SQS queue for notifications.</description>
    <schedule>every 1 minutes</schedule>
  </cron>
  <cron>
    <url>/check_fallbacks_job</url>
    <description>Check active fallbacks for overpopulation.</description>
    <schedule>every 60 minutes</schedule>
  </cron>
</cronentries>
