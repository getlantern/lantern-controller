<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
  <cron>
    <url>/persist</url>
    <description>Persist memcache entries to datastore</description>
    <!-- this must match the value in ShardedCounterManager.PERSIST_TIMEOUT -->
    <schedule>every 1 minutes</schedule>
  </cron>
  <cron>
    <url>/check_sqs</url>
    <description>Check the incoming SQS queue for notifications.</description>
    <schedule>every 1 minutes</schedule>
  </cron>
  <cron>
    <url>/query_donations</url>
    <description>Check rally.org for donations.</description>
    <schedule>every 1 minutes</schedule>
  </cron>
  <cron>
    <url>/charge_for_proxies</url>
    <description>Deduct credit for sponsored proxies. Possibly shut down underfunded ones.</description>
    <schedule>every day 23:00</schedule>
  </cron>
</cronentries>
